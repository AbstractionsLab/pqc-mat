---
active: true
authors: VECTOR team
complexity: '2'
derived: false
execution_type: Manual
expected_outcome: |
  Each algorithm component in the annotated CBOM output contains a properties entry
  with name pqcmat:risk-classification and a non-empty value.
level: 1.13
links:
- SRS-012: 94XkRO84azrXkg-s_9iIw7f2z0o-eM6XbCmlvu2LcW0=
normative: true
platform: GNU/Linux workstation or dev container
ref: ''
release: alpha
reviewed: qqq-PI7LRP5FbHuSd1KA0F5g4q6pFXDRDh83AgIuQek=
test_data: |
  tests/content/sample_tls_cbom.json — a minimal fixture containing RSA, AES-128-GCM, and ML-KEM-768 algorithm components.
verification_method: Test
version: '0.1'
---

# Verify annotated CBOM contains pqcmat risk properties

## Preconditions and setup actions
- VECTOR-Score module is installed and importable.
- `tests/content/sample_tls_cbom.json` fixture is available with at least one algorithm component.

## Test steps
1. Load `tests/content/sample_tls_cbom.json`.
2. Call `cbom_scorer.score_cbom(cbom)` to obtain the annotated CBOM.
3. For each component in the annotated CBOM where `cryptoProperties.assetType == "algorithm"`:
   a. Assert that a `properties` array is present.
   b. Assert that a `properties` entry with `name == "pqcmat:risk-classification"` exists and has a non-empty `value`.
   c. Assert that a `properties` entry with `name == "pqcmat:risk-score"` exists.
   d. Assert that a `properties` entry with `name == "pqcmat:recommended-migration"` exists.
4. Assert that the annotated CBOM `metadata.properties` contains an entry with `name == "pqcmat:scored-at"`.

## Expected outcome
All algorithm components in the annotated CBOM carry the required `pqcmat:` properties. Non-algorithm components (protocol, certificate) have no `pqcmat:` properties added.